{
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('public',{recursive:true}); for (const name of ['index.html','history.html','global.html','green.html','styles.css','script.js']) require('fs').copyFileSync(name, 'public/' + name); require('fs').cpSync('assets', 'public/assets', {recursive:true});\""
  },
  "devDependencies": {}
}
